# Library-management-system-flask
A Flask web app to manage a small library — book catalog with search, member registration/login, admin-controlled issue/return workflow, and automatic fine calculation for late returns. Built with Python, Flask, Jinja2, HTML/CSS.
